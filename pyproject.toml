[project]
name = "equity-research-assistant"
version = "0.1.0"
description = "AI-assisted research system for global listed companies, with a primary focus on US and India markets (POC)"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
